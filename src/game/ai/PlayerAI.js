const initialPlayerCode = `({
  run: function (api) {
    // Qui inseriremo la logica della nostra IA.
    // Esempio:
    // const enemy = api.scan();
    // if (enemy) {
    //   api.fire();
    // }
  },
})`;

export default initialPlayerCode;
